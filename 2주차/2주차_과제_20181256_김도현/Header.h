#include <stdio.h> //stdio.h 라이브러리를 가져옴(printf,scanf 같은 것 사용)
#include <stdlib.h> //stdlib.h 라이브러이 가져옴(malloc 같은 것 사용)

void page_calculator(int *num_count,int page_number);
//page_calculator 함수 선언
void print(int *num_count);
// print 함수 선언
